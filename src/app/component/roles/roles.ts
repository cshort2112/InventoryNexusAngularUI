import { Component } from '@angular/core';
import {AppNavbar} from "../app-navbar/app-navbar";

@Component({
  selector: 'app-roles',
    imports: [
        AppNavbar
    ],
  templateUrl: './roles.html',
  styleUrl: './roles.css',
})
export class Roles {

}
